{% extends 'base.html.twig' %}

{% block body %}

    <div class="container">
      {% for message in messages %}
      <div class="row">
        <div class="col-md-12 col-xs-12 message-box">

          <div class="message">
            {{ message.content }}
          </div>
          <div class="info">
            {{ message.author }}<span class="pull-right">{% if message.date %}{{ message.date|date('Y-m-d | H:i') }}{% endif %}</span>
          </div>

        </div>
      </div>
      {% endfor %}
    </div>

    <!-- <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Author</th>
                <th>Date</th>
                <th>Content</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for message in messages %}
            <tr>
                <td><a href="{{ path('message_show', { 'id': message.id }) }}">{{ message.id }}</a></td>
                <td>{{ message.author }}</td>
                <td>{% if message.date %}{{ message.date|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{{ message.content }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('message_show', { 'id': message.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('message_edit', { 'id': message.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('message_new') }}">Create a new message</a>
        </li>
    </ul> -->
{% endblock %}
